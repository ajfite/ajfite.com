stages:
  - build and deploy
    
build:
  stage: build and deploy
  
  script:
    - BUNDLE_GEMFILE=Gemfile bundle install --deployment --clean
    - bundle exec jekyll build -d /rootstore/wwwdata/ajfite.com/www/

  tags:
    - webserver
